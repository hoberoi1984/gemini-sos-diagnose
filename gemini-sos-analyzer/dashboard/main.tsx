import React from 'react'
import ReactDOM from 'react-dom/client'
import SOSViewer from './SOSViewer'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <SOSViewer />
  </React.StrictMode>,
)
